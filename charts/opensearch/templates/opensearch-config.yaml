{{- if .Values.opensearchConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opensearch.fullname" . }}-opensearch-config
  labels:
    {{- include "opensearch.labels" . | nindent 4 }}
data:
  opensearch.yml: |
    {{- .Values.opensearchConfig | nindent 4 }}
{{- end }}
