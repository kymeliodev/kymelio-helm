apiVersion: v1
kind: Service
metadata:
  name: {{ include "codey.fullname" . }}-headless
  labels:
    {{- include "codey.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.portName }}
      protocol: TCP
      name: {{ .Values.service.portName }}
  selector:
    {{- include "codey.selectorLabels" . | nindent 4 }}
