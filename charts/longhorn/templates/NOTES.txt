The Longhorn manager has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

The manager runs as a Deployment exposing its API on port {{ .Values.service.port }} ({{ .Values.service.portName }}).

Important: this chart installs the manager control plane only. The Longhorn
Custom Resource Definitions, the per node engine and instance manager
components, the CSI driver and the UI are installed separately and must be
present for the manager to operate.

Check the manager rollout with:

  kubectl --namespace {{ .Release.Namespace }} rollout status deploy/{{ include "longhorn.fullname" . }}

The manager API is reachable inside the cluster at:

  {{ include "longhorn.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}
