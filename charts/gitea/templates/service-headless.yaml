apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitea.fullname" . }}-headless
  labels:
    {{- include "gitea.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.portName }}
      protocol: TCP
      name: {{ .Values.service.portName }}
  selector:
    {{- include "gitea.selectorLabels" . | nindent 4 }}
