Apache CouchDB has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

Connection host inside the cluster:

  {{ include "couchdb.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

User: {{ .Values.auth.username }}

{{- if not .Values.auth.existingSecret }}

Retrieve the password with:

  kubectl --namespace {{ .Release.Namespace }} get secret {{ include "couchdb.fullname" . }} \
    -o jsonpath="{.data.{{ .Values.auth.secretKeys.passwordKey }}}" | base64 -d
{{- end }}

Port forward to your workstation:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "couchdb.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then open the Fauxton interface at:

  http://127.0.0.1:{{ .Values.service.port }}/_utils

Run the bundled connection test with:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
