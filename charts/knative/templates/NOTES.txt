Knative Serving controller has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

The Knative Serving controller is now running.

NOTE: this chart deploys only the Serving controller. The Knative Serving custom
resource definitions and the other Serving components (activator, autoscaler,
webhook and a networking layer) install separately and are required for a
working Knative Serving installation.

Check the controller is ready with:

  kubectl --namespace {{ .Release.Namespace }} rollout status deploy/{{ include "knative.fullname" . }}

Scrape controller metrics from inside the cluster at:

  {{ include "knative.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/metrics

Run the bundled connection test with:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
