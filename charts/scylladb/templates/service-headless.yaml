apiVersion: v1
kind: Service
metadata:
  name: {{ include "scylladb.fullname" . }}-headless
  labels:
    {{- include "scylladb.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.portName }}
      protocol: TCP
      name: {{ .Values.service.portName }}
  selector:
    {{- include "scylladb.selectorLabels" . | nindent 4 }}
