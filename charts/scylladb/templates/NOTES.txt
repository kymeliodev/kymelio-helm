{{ .Chart.Name }} has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

ScyllaDB exposes the CQL protocol on port {{ .Values.service.port }}.

Reach the CQL endpoint from inside the cluster at:

  {{ include "scylladb.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Connect with cqlsh from a temporary client pod:

  kubectl --namespace {{ .Release.Namespace }} run scylladb-client --rm -it --restart=Never \
    --image {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }} \
    --command -- cqlsh {{ include "scylladb.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local {{ .Values.service.port }}

To port forward to your workstation:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "scylladb.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Run the bundled connection test with:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
