apiVersion: v1
kind: Service
metadata:
  name: {{ include "couchbase.fullname" . }}-headless
  labels:
    {{- include "couchbase.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    {{- range $key, $p := .Values.service.ports }}
    - port: {{ $p.port }}
      targetPort: {{ $p.name }}
      protocol: TCP
      name: {{ $p.name }}
    {{- end }}
  selector:
    {{- include "couchbase.selectorLabels" . | nindent 4 }}
