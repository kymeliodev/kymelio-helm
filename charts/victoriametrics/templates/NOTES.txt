VictoriaMetrics has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

Reach the HTTP API inside the cluster at:

  {{ include "victoriametrics.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

To port forward the user interface to your workstation:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "victoriametrics.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then open http://127.0.0.1:{{ .Values.service.port }}/vmui in your browser.

Write Prometheus style samples to the import endpoint:

  http://127.0.0.1:{{ .Values.service.port }}/api/v1/import/prometheus

Run the bundled connection test with:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
