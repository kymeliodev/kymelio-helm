apiVersion: v1
kind: Service
metadata:
  name: {{ include "seaweedfs.fullname" . }}-headless
  labels:
    {{- include "seaweedfs.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "seaweedfs.selectorLabels" . | nindent 4 }}
