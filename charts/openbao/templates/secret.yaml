{{- if and .Values.devMode.enabled (not .Values.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openbao.fullname" . }}
  labels:
    {{- include "openbao.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.auth.secretKeys.rootTokenKey }}: {{ include "openbao.rootToken" . | b64enc | quote }}
{{- end }}
