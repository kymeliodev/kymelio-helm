cert-manager has been installed.

Release name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

The cert-manager controller is now running. Confirm the deployment is ready:

  kubectl --namespace {{ .Release.Namespace }} rollout status deployment/{{ include "cert-manager.fullname" . }}

Reminder: this chart does not install the cert-manager Custom Resource
Definitions. Make sure the CRDs for the cert-manager API types are installed
before creating any Issuer, ClusterIssuer or Certificate resources.

Controller metrics are exposed on the {{ .Values.service.portName }} port:

  {{ include "cert-manager.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Run the bundled connection test with:

  helm test {{ .Release.Name }} --namespace {{ .Release.Namespace }}
